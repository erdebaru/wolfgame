import { createInterface } from "readline/promises";

import {
  Game,
  Player,
  Round,
  Message,
} from "./game";

const readline = createInterface({
  input: process.stdin,
  output: process.stdout
});

async function main() {
  
}

main();